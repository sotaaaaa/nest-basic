export const AppConstants = {
  // Cấu hình mặc định cho validator
  validatorOptions: {
    forbidUnknownValues: true,
    whitelist: true,
    forbidNonWhitelisted: true,
  },

  // Cấu hình lỗi cho hệ thống
  errors: {
    defaultMessage: 'An error occurred',
  },
};
